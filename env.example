# Asana MCP HTTP Server Configuration

# Server Configuration
NODE_ENV=production
PORT=10000

# Asana Configuration
# Get your personal access token from: https://app.asana.com/0/my-apps
ASANA_ACCESS_TOKEN=your_asana_personal_access_token_here

# Optional: Enable read-only mode (only allows read operations)
# READ_ONLY_MODE=false
